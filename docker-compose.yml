services:
  bot:
    build: .
    container_name: support_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./images:/app/images:ro
    command: python -m watchfiles --filter python "python -m app.main" /app/app
